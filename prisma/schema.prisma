//🧩Don't Edit this file.✨Generated in Wed Apr 17 2024 12:47:30 GMT+0600 (Bangladesh Standard Time)✨

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Admin {
  id        String   @id @default(uuid())
  name      String
  email     String   @unique
  password  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Meal {
  id                String              @id @default(uuid())
  meal              MealType
  mealNumber        String // @Note: is it number or string?
  mealName          String
  preparationMethod PreparationMethod[]
  cookingTime       String
  tips              Tips[]
  taxPercent        TaxPercent?
  shortDescription  String?
  label             String?
  subLabel          String?
  image             String

  ingredients Ingredient[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  weeklyMeals WeeklyMeal[]

  @@index([mealName])
}

enum MealType {
  breakfast
  dinner
  lunch
  snacks1
  snacks2
  snacks3
}

model Ingredient {
  id            String             @id @default(uuid())
  name          String
  quantity      Int
  unit          IngredientUnitType
  kcal          Int
  proteins      Int
  carbohydrates Int
  fats          Int
  fiber         Int
  categoryId    String?

  meals    Meal[]
  category IngredientCategory? @relation(fields: [categoryId], references: [id])

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([name])
}

model IngredientCategory {
  id String @id @default(uuid())

  name      String @unique
  slug      String @unique
  sortOrder Int    @default(autoincrement())

  ingredients Ingredient[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([name, slug])
}

enum IngredientUnitType {
  gr
  ml
  por
  pc
  x
}

model PreparationMethod {
  id String @id @default(uuid())

  label String

  // relation
  mealId String
  meal   Meal   @relation(fields: [mealId], references: [id])

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Tips {
  id String @id @default(uuid())

  label String

  // relation
  mealId String
  meal   Meal   @relation(fields: [mealId], references: [id])

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model WeeklyMeal {
  id    String @id @default(uuid())
  week  Int    @unique
  meals Meal[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([week])
}

model Order {
  id              String      @id @default(uuid())
  amount          Float
  shippingAmount  Float       @default(0)
  coupon          Json?
  orderId         String?     @unique
  userId          String
  shippingAddress Json
  status          OrderStatus @default(processing)
  sessionId       String?

  user                       User                       @relation(fields: [userId], references: [id])
  reviews                    ProductReview[]
  productReviewOpportunities ProductReviewOpportunity[]
  orderItems                 OrderItem[]

  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  paidAt    DateTime?

  @@index([amount])
}

model OrderItem {
  id String @id @default(uuid())

  orderId String
  order   Order  @relation(fields: [orderId], references: [id])

  productId String?
  product   Product? @relation(fields: [productId], references: [id], onDelete: SetNull)

  name        String
  price       Float
  quantity    Int
  taxPercent  TaxPercent?
  attributes  Json?
  image       String
  variationId String?
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt

  @@index([orderId, productId, name, variationId])
}

enum OrderStatus {
  unpaid
  processing
  completed
}

model UserPlan {
  id String @id @default(uuid())

  numberOfDays Int
  mealsPerDay  Int

  confirmOrderWeek Int?

  status PlanStatus @default(pending)

  orders PlanOrder[]

  userId String @unique
  user   User   @relation(fields: [userId], references: [id])

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([numberOfDays, mealsPerDay, userId])
}

enum PlanStatus {
  pending
  active
  canceled
}

model PlanOrder {
  id String @id @default(uuid())

  week Int

  mealsForTheWeek Json
  totalAmount     Float
  shippingAmount  Float

  lockdownDate DateTime?
  deliveryDate DateTime?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  plan   UserPlan? @relation(fields: [planId], references: [id])
  planId String?

  status PlanOrderStatus @default(confirmed)

  @@index([totalAmount, week])
  @@index([lockdownDate, deliveryDate])
}

enum PlanOrderStatus {
  confirmed
  delivered
}

enum TaxPercent {
  TAX9  @map("9")
  TAX21 @map("21")
}

model Product {
  id                              String      @id @default(uuid())
  name                            String
  slug                            String      @unique
  description                     String?
  longDescription                 String?
  salePrice                       Float?
  regularPrice                    Float?
  images                          String[]
  stock                           Int?
  lowStockThreshold               Int?
  faqs                            Json?
  specs                           Json?
  showOnBestSellerSection         Boolean     @default(false)
  showOnCartRecommendationSection Boolean     @default(false)
  taxPercent                      TaxPercent?
  linkedToId                      String?
  attributesInfo                  Json?
  lowestPrice                     Float?
  highestPrice                    Float?

  linkedProducts           Product[]                  @relation("LinkedProducts")
  linkedTo                 Product?                   @relation("LinkedProducts", fields: [linkedToId], references: [id])
  reviews                  ProductReview[]
  productReviewOpportunity ProductReviewOpportunity[]
  type                     ProductType
  productCart              ProductCart[]
  attributes               ProductAttribute[]
  attributeTerms           ProductAttributeTerm[]
  variations               ProductVariation[]
  categories               ProductCategory[]
  spotlightsProductBanner  SpotlightsProductBanners?
  orderedItems             OrderItem[]

  createdAt          DateTime  @default(now())
  updatedAt          DateTime  @updatedAt
  lowStockNotifiedAt DateTime?

  @@index([slug, name, description, salePrice, regularPrice, stock, showOnBestSellerSection, showOnCartRecommendationSection])
}

model ProductReview {
  id        String  @id @default(uuid())
  comment   String
  rating    Float
  productId String
  userId    String
  orderId   String
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)
  user      User    @relation(fields: [userId], references: [id])
  order     Order   @relation(fields: [orderId], references: [id])

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([userId, rating, productId, orderId])
}

model ProductReviewOpportunity {
  id String @id @default(uuid())

  productId String?
  userId    String
  orderId   String

  product Product? @relation(fields: [productId], references: [id], onDelete: Cascade)
  user    User     @relation(fields: [userId], references: [id])
  order   Order    @relation(fields: [orderId], references: [id])

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([userId, productId, orderId])
}

model SpotlightsProductBanners {
  id        String  @id @default(uuid())
  title     String
  image     String
  productId String  @unique
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model ProductCategory {
  id               String            @id @default(uuid())
  name             String            @unique
  image            String?
  description      String?
  slug             String            @unique
  sortOrder        Int               @default(autoincrement())
  parentCategoryId String?
  parentCategory   ProductCategory?  @relation("CategoryToParent", fields: [parentCategoryId], references: [id])
  subCategories    ProductCategory[] @relation("CategoryToParent")

  products Product[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([name, slug])
}

enum ProductType {
  simple
  variable
}

model ProductVariation {
  id String @id @default(uuid())

  termIds String[]

  salePrice              Float?
  regularPrice           Float?
  stock                  Int?
  lowStockThreshold      Int?
  image                  String?
  imageSameAsVariationId String?
  createdAt              DateTime  @default(now())
  updatedAt              DateTime  @updatedAt
  lowStockNotifiedAt     DateTime?

  terms       ProductAttributeTerm[]
  product     Product?               @relation(fields: [productId], references: [id], onDelete: Cascade)
  productId   String?
  ProductCart ProductCart[]

  @@index([productId, salePrice, regularPrice, stock])
}

model ProductAttribute {
  id    String                 @id @default(uuid())
  name  String                 @unique
  slug  String                 @unique
  terms ProductAttributeTerm[]

  products Product[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([name, slug])
}

model ProductAttributeTerm {
  id        String   @id @default(uuid())
  name      String   @unique
  slug      String   @unique
  sortOrder Int      @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  attribute          ProductAttribute? @relation(fields: [productAttributeId], references: [id], onDelete: Cascade)
  productAttributeId String?

  products Product[]
  // productId String?

  productVariation   ProductVariation? @relation(fields: [productVariationId], references: [id])
  productVariationId String?

  @@index([name, slug])
}

model ProductCart {
  id String @id @default(uuid())

  count Int @default(1)

  productId String
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)
  user      User?   @relation(fields: [userId], references: [id])
  userId    String?

  variationId String?
  variation   ProductVariation? @relation(fields: [variationId], references: [id], onDelete: Cascade)

  guestId String?

  placed Boolean @default(false)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([userId, productId, guestId])
}

model Coupon {
  id String @id @default(uuid())

  name String
  code String @unique

  type  CouponType
  value Int

  policy CouponPolicy

  status CouponStatus @default(active)

  users User[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([code, policy, status])
}

enum CouponStatus {
  active
  inactive
}

enum CouponType {
  percent
  amount
}

enum CouponPolicy {
  onetime
  multiple
}

model Token {
  id        String    @id @default(uuid())
  token     String    @unique
  data      String
  type      TokenType
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
}

enum TokenType {
  ConfirmPayment
  ConfirmEmail
  ResetPassword
}

model User {
  id                    String                 @id @default(uuid())
  // personal info
  name                  String
  profile               String? // profile image
  surname               String?
  age                   Int?
  gender                Gender?
  weight                Int?
  height                Int?
  activityLevel         String?
  goal                  String?
  userNextWeekPlanPrice UserNextWeekPlanPrice?
  // address info
  address               String?
  nr                    String?
  city                  String?
  addition              String?

  // credentials
  email                    String                     @unique
  mobile                   String?
  password                 String?
  zipCodeId                String?
  zipCode                  ZipCode?                   @relation("UserZipCode", fields: [zipCodeId], references: [id])
  productDeliveryZipCode   String?
  status                   UserStatus                 @default(active)
  productReviews           ProductReview[]
  productReviewOpportunity ProductReviewOpportunity[]

  access UserAccess @default(product)

  customer String? // stripe customer id

  plan UserPlan?

  carts ProductCart[]

  coupons Coupon[]

  verified  Boolean  @default(false)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  orders    Order[]

  @@index([email, mobile, name, surname, zipCodeId, password])
}

enum UserAccess {
  product
  all
}

enum Gender {
  male
  female
  others
}

enum UserStatus {
  active
  blocked
}

model ZipCode {
  id          String @id @default(uuid())
  lockdownDay Int
  zipCode     String @unique

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  users User[] @relation("UserZipCode")

  @@index([zipCode, lockdownDay])
}

model UserNextWeekPlanPrice {
  id String @id @default(uuid())

  userId String @unique
  user   User   @relation(fields: [userId], references: [id])

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model RawData {
  id         String @id @default(uuid())
  identifier String @unique
  data       Json

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
